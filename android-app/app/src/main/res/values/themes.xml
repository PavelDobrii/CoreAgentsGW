<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Use a platform theme that does not depend on external design libraries. -->
    <style name="Theme.CityGuide" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@color/md_theme_primary</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/md_theme_primary</item>
    </style>

    <!-- Backwards compatibility alias so any references to the previous Material3 theme resolve locally. -->
    <style name="Theme.Material3.DayNight.NoActionBar" parent="@style/Theme.CityGuide" />
</resources>
